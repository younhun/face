import { StyleSheet } from 'react-native';

export default homeStyles = StyleSheet.create({
  container: {
    flex: 1, 
  },

  btnContainer: {
    marginTop: 20,
    alignItems: 'center',
    justifyContent: 'center'
  }


});